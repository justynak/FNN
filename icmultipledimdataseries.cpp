#include "icmultipledimdataseries.h"

/*
template< class T >
ICMultipleDimDataSeries<T>::ICMultipleDimDataSeries()
{
    _data = QList<T>();
}

template< class T >
ICMultipleDimDataSeries<T>::~ICMultipleDimDataSeries()
{
    _data.clear();
}

template< class T >
bool ICMultipleDimDataSeries<T>::AttachPoint(T p)
{
    this->_data.append(p);
    return true;
}

template< class T >
bool ICMultipleDimDataSeries<T>::RemovePoint(T p)
{
    return _data.removeOne(p);
}

template< class T >
bool ICMultipleDimDataSeries<T>::ReplacePoint(T p, int i)
{
    return _data.replace(i, p);
}

template< class T >
T ICMultipleDimDataSeries<T>::operator[](int number)
{
    if(number < _data.size())
        return _data[number];
    else
        return T();
}

template< class T >
int ICMultipleDimDataSeries<T>::GetSampleNumber()
{
    return _data.size();
}
*/

